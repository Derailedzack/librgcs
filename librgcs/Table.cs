using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ClassLibrary1
{
    //This will include a parial port of zpl
    //Table might not be neccesary because I could just a list
    /* internal class Table<T>
     {
         uint hashes;
         T entries;
         private List<uint> zpl_ah = new List<uint>();
         private List<T> zpl_ah_2 = new List<T>();
         private List<TableEntry> tableEntries = new List<TableEntry>();
         void TableInit(Table<T> h)
         {
             do
             {
                 uint zpl__array = h.hashes;

                 zpl_ah.Add(hashes);

                 zpl_ah_2.Add(entries);

             } while (true);


         }
         void TableDestroy(Table<T> table)
         {
             if (entries != null)
             {
                 entries = default(T);
                 zpl_ah = null;
                 zpl_ah_2 = null;
             }
         }
         uint AddTableEntry(Table<T> h,UInt64 key)
         {
             uint index;
             TableEntry e = new TableEntry();
             e.key = key;
             e.next = -1;

             tableEntries.Add(e);
             //index = entries
             return 0x00;

         }

         HashSet<T> FindTable(Table<T> h,UInt64 key)
         {
             HashSet<T>
         }
     }
     internal class TableEntry
     {
         public UInt64 key;
         public int next;
         public object value;

     }*/

}
